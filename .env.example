# ---------------------------------------------------------
# 🔐 Environment configuration for development and testing
# ---------------------------------------------------------
# These keys are required to sign and verify JWTs
# during local development, unit testing, and examples.
#
# The keys must be:
# - In PKCS#8 PEM format
# - Base64-encoded (e.g. via `base64 -w 0 ec-private.pem`)
#
# You may load them from a secure location in production
# (e.g. AWS Secrets Manager, Vault, or env vars at runtime)

JWT_PRIVATE_KEY="your_base64_encoded_private_key_here"
JWT_PUBLIC_KEY="your_base64_encoded_public_key_here"